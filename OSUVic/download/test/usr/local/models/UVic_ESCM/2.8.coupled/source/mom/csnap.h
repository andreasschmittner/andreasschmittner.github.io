!====================== include file "csnap.h" =========================

!     snapls = starting latitude (degs) for snapshots
!     snaple = ending latitude (degs) for snapshots
!     snapde = ending depth (centimeters) for snapshots

      common /csnap/ snapls, snaple, snapde
